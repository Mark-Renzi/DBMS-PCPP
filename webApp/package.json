{
  "name": "nodebootstrapper",
  "version": "5.0.0",
  "dependencies": {
    "body-parser": "*",
    "dotenv": "^16.3.1",
    "express": "*",
    "method-override": "*",
    "morgan": "*",
    "pg": "^8.11.3",
    "pug": "*",
    "serve-favicon": "*",
    "stylus": "*"
  },
  "engines": {
    "node": "*",
    "npm": "*"
  },
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  }
}
